//============ IMPORTS ============
import React from "react";
import "./header.css";

//============ CONTENT ============
const Header = (props) => (
    <header className="header">
        <h2>Click on a princess, but don't click on her more than once</h2>
    </header>
);

//============ EXPORT ============
export default Header;