//============ IMPORTS ============
import React from "react";
import "./wrapper.css";

//============ CONTENT ============
const Wrapper = (props) => (
    <main className="wrapper">{props.childern}</main>
);

//============ EXPORT ============
export default Wrapper;